# Проверка макета · 22 сентября 2026

Проверены в браузере:

- Первый экран, отдельная карта и увеличение штрихкода.
- Поиск неизвестного товара: пустое состояние и сброс; поиск Pilsner.
- Добавление в список, увеличение количества, расчёт 2 × 180 ₽.
- Создание локального запроса и демонстрация подтверждения магазином.
- Обмен 180 бонусов на купон и отмена: баланс восстановился до 1 240.
- Подбор: мягкий вкус, без алкоголя, бюджет до 200 ₽. Возвращены безалкогольные варианты.
- Фильтр «Мягкий»: показаны 4 товара в демонстрационном каталоге.
- Оценка покупки 5/5: оценка отображается в истории.
- Обращение о карте: форма сохраняет текст, возвращает номер, не отправляет данные.
- Алтайская: выдача наград блокируется, предлагается сменить магазин.
- Переключатель деморежима без сети: штрихкод и карточка остаются доступными.
- Сброс демонстрации возвращает исходные данные.
- Экраны 390 × 844, 1280 × 800, 1440 × 1000. На проверенных мобильном и настольном экранах горизонтального переполнения документа нет; изображения загружены.
- В финальной проверке консоли нет ошибок JavaScript.

Дополнительно: синтаксис app.js и sw.js проверен Node; относительные ссылки двух HTML-страниц разрешаются; упоминания прежнего бренда отсутствуют.

Физическое отключение сети, реальные push-уведомления, касса и сервер не тестировались. Реальные интеграции в макете отсутствуют. Сервис-воркер включён для кэширования статических файлов после первого успешного посещения.

## Повторная проверка после редизайна

- Flanders: фильтр показывает 16 товаров, карточка Helles отображает 4,5% и 15 IBU, источник и пометку демоцены; добавление в список работает.
- Карта: выбор второй метки обновляет выделение и адрес, увеличение и возврат масштаба работают, перемещение стрелками работает.
- Центральная кнопка находится внутри границ нижней панели. Экран сохраняет 382:826 при масштабировании.
- Диалог после масштабирования совпадает по ширине и левому краю с экраном телефона.
- Персональный купон доступен из главной и «Моих купонов». Предпросмотр показывает демонстрационный штрихкод и условия.
- Новые изображения афиш: 1 068 174 → 245 734 байта; купон 1 163 079 → 104 632 байта.

- После изменения товарной подачи проверены бутылки с этикетками и пять напитков с фоном-плакатом.
- 390 × 844: нет горизонтального переполнения, центральная кнопка полностью внутри навигации, нет сломанных загруженных изображений; консоль без ошибок.
- В «Выгодах» персональный купон отображается перед балансом и вкладками.
- Клиентская презентация визуально проверена в браузере, ссылки ведут в макет.

## Motion update verification
- Syntax checks: app.js, motion.js, sw.js.
- Local browser: catalog product opening, sheet dismissal by downward drag, Benefits coupon opening, Escape dismissal, return home.
- No browser console errors; transition snapshots removed after completion.
- Reduced-motion and hidden-document cleanup inspected in code; OS preference not emulated.

Motion tuning: syntax checks passed; catalog/benefits/home navigation rechecked locally. No duplicated transition DOM or hidden catalog cards. Bottle label hover no longer inherits the legacy rotated-image rule.
